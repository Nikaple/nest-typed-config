isAuthEnabled: true
database:
  host: 127.0.0.1
  port: 3000
  table:
    name: ${TABLE_NAME}

databaseAlias:
  host: ${database.host}
  port: ${database.port}
  table:
    name: ${database.table.name}
