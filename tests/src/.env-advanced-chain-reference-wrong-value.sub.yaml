isAuthEnabled: true
database:
  host: ${database.noValue}
  port: 3000
  table:
    name: ${TABLE_NAME}

databaseAlias:
  host: ${databaseAlias.host}
  port: ${database.port}
  table:
    name: ${database.table.name}
