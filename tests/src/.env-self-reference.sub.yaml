isAuthEnabled: true
database:
  # this one should fail
  host: ${database.host}
  port: 3000
  table:
    name: ${TABLE_NAME}

databaseAlias:
  host: ${database.host}
  port: ${database.port}
  table:
    name: ${database.table.name}
