isAuthEnabled: true
database:
  host: localhost
  port: ${databaseAlias.port}
  table:
    name: ${TABLE_NAME}

databaseAlias:
  host: ${database.host}
  port: 3000
  table:
    name: ${database.table.name}
