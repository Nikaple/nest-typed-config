isAuthEnabled: true
isAuthEnabledCopy: ${isAuthEnabled}
database:
  host: 127.0.0.1
  port: 3000
  table:
    name: users

databaseAlias:
  host: ${database.host}
  port: ${database.port}
  table:
    name: ${database.table.name}
